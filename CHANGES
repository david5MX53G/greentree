* refactored GreenTreeServer such that SocketServer executes in the main thread and the GreenTreeServerHandler processing of each Socket is run in a separate, dedicated thread
* moved GreenTreeServer port assignment out of the class and into application.properties.xml, accessing it via PropertyManager.getProperty(String)